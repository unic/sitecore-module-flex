mode: ContinuousDeployment
branches:  
   master:  
      regex: ^master$|^main$
   sc104xm:
      regex: ^feature/sitecore-104-xm$
      mode: ContinuousDelivery
      increment: Major
      tag: "sc104xm"
      source-branches: []