@using Unic.Flex.Presentation
@model Unic.Flex.Model.DomainModel.Steps.StepBase


---------------------------------------
@Model.Title
---------------------------------------

@foreach (var standardSection in @Model.Sections.Where(s => !s.IsHidden))
{
    @Html.FlexComponent(m => standardSection, "Mailers/SavePlug/Section.text", (string)ViewBag.Theme)
}