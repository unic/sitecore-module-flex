@using Unic.Flex.Presentation
@inherits Unic.Flex.Presentation.FlexView<Unic.Flex.Model.ViewModel.Sections.SectionViewModel, Unic.Flex.Model.DomainModel.Sections.StandardSection>

@if (Model.DisableFieldset)
{
    <ul>
        <li>Fields without fieldset</li>
    </ul>
}
else
{
    <fieldset>
        <legend>@Model.Title</legend>
        
        <ul>
            @for(int i=0; i<Model.Fields.Count; i++)
            {
                @Html.FlexComponent(m => m.Fields[i])
            }
        </ul>

    </fieldset> 
}