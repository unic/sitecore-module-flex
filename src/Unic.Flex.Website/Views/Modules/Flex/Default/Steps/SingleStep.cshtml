@using Unic.Flex.Presentation
@inherits Unic.Flex.Presentation.FlexView<Unic.Flex.Model.Steps.StepViewModel, Unic.Flex.Model.Steps.SingleStep>

@for (int i = 0; i < Model.Sections.Count; i++)
{
    @Html.FlexComponent(Model.Sections[i], ViewData.TemplateInfo.HtmlFieldPrefix + ".Sections[" + i + "]")
}

<input type="submit" value="submit" />